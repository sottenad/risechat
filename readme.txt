Install node.js
Install NPM
in the node console, cd to your project directory
Run `npm install`
    You might need to run `npm install socket.io --msvs_version=2013` if your install fails
	You might need to run 'npm install request' as well
after that finishes, run `nodemon server.js`
goto http://localhost:3000